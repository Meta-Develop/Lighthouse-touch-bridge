namespace Ltb.App;

/// <summary>
/// Provides the placeholder coordinator entry point until the Windows UI framework is selected.
/// </summary>
internal static class Program
{
    private static int Main()
    {
        Console.WriteLine("Lighthouse Touch Bridge application placeholder.");
        return 0;
    }
}
